// Vignette disabled — the corner darkening read as ugly gray on light pages.
// Kept as a no-op so the ~20 pages that import <Vignette /> don't need editing.
export default function Vignette() {
  return null
}
